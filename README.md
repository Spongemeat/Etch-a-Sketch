# Etch-a-Sketch
Project to practice using CSS (flexbox) and Javascript

Designing an etch a sketch program using only CSS and Javascript. Only flexbox, no grid.

Grid will be created dynamically through the DOM